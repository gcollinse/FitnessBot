[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.build]
cmds = [
  "rm -f package-lock.json",
  "npm install",
  "npm run build"
]

[phases.start]
cmd = "uvicorn server:app --host 0.0.0.0 --port $PORT"
